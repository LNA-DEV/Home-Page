{{- /*
  Content adapter for the en model pages — one file per language, because Hugo
  scopes an adapter to the language of its filename. All three are identical; the
  language-dependent part is inside the partial. See model-pages.html.
*/ -}}
{{- partial "model-pages.html" . -}}
