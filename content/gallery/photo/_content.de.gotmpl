{{- /*
  Content adapter for the per-photo pages — one file per language, because Hugo
  scopes an adapter to the language of its filename. All three are identical;
  the language-dependent part is inside the partial. See gallery-photo-pages.html.
*/ -}}
{{- partial "gallery-photo-pages.html" . -}}
